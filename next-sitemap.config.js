/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://dewett.ca", // Your website URL
  generateRobotsTxt: true, // (Optional) Generates robots.txt file
};

module.exports = config;
